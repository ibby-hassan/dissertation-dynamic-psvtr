import { style } from "@vanilla-extract/css";

export const canvasToolbar = style({
    display: "flex",
    width: "100%",
    justifyContent: "space-between",
    alignItems: "center",
    padding: "1%",
});
