import { menuSection, title } from "./styles/menuSection.css.ts";

const MenuSection = () => {
    return (
        <div className={menuSection}>
            <h1 className={title}>PSVT:R Shape Builder</h1>
            
        </div>
    )
}
export default MenuSection;